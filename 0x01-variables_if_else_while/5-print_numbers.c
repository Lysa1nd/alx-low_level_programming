#include<stdio.h>
/**
 * main - Entry point
 * Description: 'print numbers from 0 to 9'
 * Return: Always 0
 */
int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
	{
		printf("%d", m);
	}
	putchar('\n');
	return (0);
}
